PREFIX wifi: <http://example.org/wifi#>

SELECT ?punto ?ssid ?barrio
WHERE {
  ?punto a wifi:PuntoDeAccesoWiFi ;
         wifi:nombreSSID ?ssid ;
         wifi:barrio ?barrio .
  FILTER(CONTAINS(LCASE(?barrio), "palermo"))
}