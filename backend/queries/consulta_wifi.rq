PREFIX wifi: <http://example.org/wifi#>
SELECT ?provider ?lat ?lon ?city ?isOpen ?isSecure
WHERE {
    ?wifi a wifi:WiFi ;
        wifi:provider ?provider ;
        wifi:lat ?lat ;
        wifi:lon ?lon ;
        wifi:city ?city ;
        wifi:isOpen ?isOpen ;
        wifi:isSecure ?isSecure .
}